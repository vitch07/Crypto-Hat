/** @type {import('next').NextConfig} */
const nextConfig = {
  // Add your Next.js configuration options here
  reactStrictMode: true,
  // Example: Enable image optimization
  images: {
    domains: ["example.com"],
  },
};

export default nextConfig;
